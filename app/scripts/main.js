var suitRed = '#FF0000';var suitBlack = '#000000';var cardHeight = 100;var cardWidth = 60;var margin = 10;var canvas = Snap(cardWidth * (52 + margin), cardHeight);var totalCardWidth = cardWidth + margin;function renderCard(x, y, suit, value) {  var suitColor = '';  switch (suit) {    case 'hearts':    case 'diamonds':      suitColor = suitRed;    break;    case 'clubs':    case 'spades':      suitColor = suitBlack;    break;  }  Snap.load('./images/hearts.svg', function(img) {    var centerX = x + cardWidth / 2;    var centerY = y + cardHeight / 2;    var cardGroup = canvas.group();    var card = canvas.rect(0, 0, cardWidth, cardHeight, 5, 5).attr({fill: '#EEEEEE', stroke:'#999999', strokeWidth: 1});    var cardTransform = new Snap.Matrix();    cardTransform.translate(x, y);    cardGroup.transform(cardTransform);    canvas.text(centerX, centerY, value).attr({fill: suitColor});    var g = canvas.group();    g.append(img);    var suiteWidth = g.getBBox().width;    var scaleFactor = (cardWidth / 3) / suiteWidth;    var t = new Snap.Matrix();    t.translate(cardWidth / 2, cardHeight / 2);    t.scale(scaleFactor, scaleFactor);    g.transform(t);        cardGroup.append(card);    cardGroup.append(g);  });/*  var pips = [];  var deltaY = cardHeight / value;  for (var i = 0; i < value; i++) {    pips.push(card.circle(cardWidth/2, deltaY * i, 10));  }*/}var suits = ['spades', 'hearts', 'clubs', 'diamonds'];var cardCount = 0;for (var i = 0; i < suits.length; i++) {  for (var j = 2; j < 15; j++) {    renderCard(totalCardWidth * cardCount, 0, suits[i], j);    cardCount++;  }}