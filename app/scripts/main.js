var suitRed = '#FF0000';var suitBlack = '#000000';var cardHeight = 100;var cardWidth = 60;var margin = 10;var canvas = Snap(cardWidth * (52 + margin), cardHeight);var totalCardWidth = cardWidth + margin;function createCard(suit, value) {  var card = {};  card.suit = suit;  card.value = value;  //var suitColor = '';  switch (suit) {    case 'hearts':      card.color = suitRed;      card.img = './images/hearts.svg';      break;    case 'diamonds':      card.color = suitRed;      card.img = './images/diamonds.svg';    break;    case 'clubs':      card.color = suitBlack;      card.img = './images/clubs.svg';    break;    case 'spades':      card.color = suitBlack;      card.img = './images/spades.svg';    break;  }  switch (value) {    case 15:      card.name = 'A';      break;    case 14:      card.name = 'K';      break;    case 13:      card.name = 'Q';      break;    case 13:      card.name = 'J';      break;    default:      card.name = value;  }  console.log(card);  return card;}function renderCard(x, y, card) {  Snap.load(card.img, function(img) {    // TODO(bradleybossard): Clean up variable names.    // TODO(bradleybossard): Logic to color suit image properly.    var centerX = x + cardWidth / 2;    var centerY = y + cardHeight / 2;    var cardGroup = canvas.group();    var cardRect = canvas.rect(0, 0, cardWidth, cardHeight, 5, 5).attr({fill: '#EEEEEE', stroke:'#999999', strokeWidth: 1});    var cardTransform = new Snap.Matrix();    cardTransform.translate(x, y);    cardGroup.transform(cardTransform);    canvas.text(centerX, centerY, card.name).attr({fill: card.color});    var g = canvas.group();    g.append(img);    var suiteWidth = g.getBBox().width;    var scaleFactor = (cardWidth / 3) / suiteWidth;    var t = new Snap.Matrix();    t.translate(cardWidth / 2, cardHeight / 2);    t.scale(scaleFactor, scaleFactor);    g.transform(t);        cardGroup.append(cardRect);    cardGroup.append(g);  });/*  var pips = [];  var deltaY = cardHeight / value;  for (var i = 0; i < value; i++) {    pips.push(card.circle(cardWidth/2, deltaY * i, 10));  }*/}var suits = ['spades', 'hearts', 'clubs', 'diamonds'];var cardCount = 0;var cards = [];for (var i = 0; i < suits.length; i++) {  for (var j = 2; j < 15; j++) {    var card = createCard(suits[i], j);    renderCard(totalCardWidth * cardCount, 0, card);    cards.push(card);    cardCount++;  }}